/**
 * The console runs on the dark palette. Re-exported as `C` so dashboard
 * components stay palette-agnostic; the source of truth is lib/ui/theme.
 */
export { dark as C, SEV, TYPE_META, riskColor, riskBand, severityForScore, initials, hhmmss } from "@/lib/ui/theme";
